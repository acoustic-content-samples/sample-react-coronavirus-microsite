{
  "name": "acoustic-website-react-sample",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "homepage": "/7e9fa8ae-a6ba-4f4a-8518-ea37fb16413e/coronavirus-response",
  "dependencies": {
    "@material-ui/core": "4.6.0",
    "@material-ui/icons": "4.5.1",
    "react": "16.11.0",
    "react-dom": "16.11.0",
    "react-image-gallery": "1.0.3",
    "react-router-dom": "5.1.2",
    "react-router-hash-link": "^1.2.2",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "node-sass": "4.13.1",
    "shelljs": "0.8.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "hub-build": "npm run build && node copyBuildFiles",
    "hub-deploy": "wchtools push -A -v -I --dir build --publish-now",
    "hub-build-deploy": "npm run hub-build && npm run hub-deploy",
    "init-content": "wchtools push -A -I -v --dir content-artifacts --publish-now",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
